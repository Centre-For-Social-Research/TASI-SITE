export const metadata = {
  title: 'Cookie Settings | TASI 2026',
  robots: {
    index: false,
    follow: false,
  },
};

export default function CookieSettingsLayout({ children }) {
  return children;
}
