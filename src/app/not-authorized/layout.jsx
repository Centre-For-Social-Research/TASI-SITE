export const metadata = {
  title: 'Not Authorized | TASI 2026',
  robots: {
    index: false,
    follow: false,
  },
};

export default function NotAuthorizedLayout({ children }) {
  return children;
}
